// lib/domains/brief.js — Phase A stub. Phase B fills in.
const briefDomain = {};
if (typeof module !== "undefined" && module.exports) {
  module.exports = { briefDomain };
} else if (typeof window !== "undefined") {
  window.briefDomain = briefDomain;
}
